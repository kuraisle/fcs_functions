.. highlight:: shell

============
Installation
============


Stable release
--------------

To install FCS functions, run this command in your terminal:

.. code-block:: console

    $ pip install fcs_functions

This is the preferred method to install FCS functions, as it will always install the most recent stable release.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for FCS functions can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/kuraisle/fcs_functions

Or download the `tarball`_:

.. code-block:: console

    $ curl -OJL https://github.com/kuraisle/fcs_functions/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/kuraisle/fcs_functions
.. _tarball: https://github.com/kuraisle/fcs_functions/tarball/master
